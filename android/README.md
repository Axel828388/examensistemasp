Room entities and DAOs for RRHH schema

Files generated:
- Entities: User, AuditLog, Document, Meeting, Announcement, Event, Message, SensorPosition, Image
- DAOs: UserDao, AuditLogDao, DocumentDao, MeetingDao, AnnouncementDao, EventDao, MessageDao, SensorPositionDao, ImageDao

Copy the `kotlin` files into your Android Studio project's `app/src/main/java/<your package>/data` folder and add Room dependencies.
